# Without running the following code, what do you think it will do?

def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello') # Exception raised. foo needs two arguments, but only one is passed.